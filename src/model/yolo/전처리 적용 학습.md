1. 원본 데이터 학습 모델 mAP 점수
    - 그냥 train.py 실행

2. 선명도 조작 데이터 학습 모델 mAP 점수
    - yolo_imge_convert.py 실행
    - data\yolo_dataset\converted 경로에 전처리 이미지 생성
    - train 이미지에 대해 선명도 향상 전처리 적용
    - val, test 이미지는 적용 안함(원본 이미지 사용)

    - yolo_train_converted.py 실행
    - 전처리 한 이미지로 학습하는 전용 스크립트

3. 원본 + 증강 데이터 학습 모델 mAP 점수
    - yolo_experiment_data.py 실행
    - train 및 val 이미지에 대해 증강 실행
    - data\yolo_dataset\experiment3 경로에 증강된 이미지 생성

    - yolo_train_experiment.py 실행
    - 증강한 이미지로 학습하는 전용 스크립트

4. 원본 + 증강(전처리) 데이터 학습 모델 mAP 점수
    - yolo_experiment2_data.py 실행
    - train 및 val 이미지에 대해 증강 및 선명도 전처리 실행
    - data\yolo_dataset\experiment4 경로에 증강 및 전처리된 이미지 생성

    - yolo_train_experiment2.py 실행
    - 증강 및 전처리한 이미지로 학습하는 전용 스크립트


학습에서 고친거 - best.pt 자동으로 교체되게
